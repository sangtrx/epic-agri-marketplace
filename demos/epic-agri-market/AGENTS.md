# EPIC Agri Market application

This app was generated by the official Mercur 2.x starter and lives separately from the upstream development monorepo. Keep marketplace core upstream-friendly; adapt this app rather than editing the fork's `packages/core`.

Read version-matched bundled documentation before nontrivial changes: `node_modules/@mercurjs/docs/llms.txt`, then the referenced `content/**/*.mdx` pages. Consult `README.md` and `VALIDATION.md` for run commands and accepted checks.

Use `bun install --cwd .` from this directory to prevent ancestor-workspace resolution. Generate API types with `bun run --cwd packages/api codegen`. Backend: 9107; buyer: 3107; internal admin/vendor Vite: 7107/7108. All development listeners must remain loopback.

Use only fictional demo contacts and coarse regions. Preserve private `.env` and `.demo` files; never print or commit their contents. No map, paid integration, real payout, verification or inspection claims are required for this demo.
